## Issue

Closes #

## What changed

## Tests

- [ ] `mci-core`: `./gradlew test` (Windows: `gradlew.bat test`)
- [ ] `mci-web`: `./gradlew test` (Windows: `gradlew.bat test`)
- [ ] N/A — docs or templates only

## AI

- [ ] No AI used
- [ ] AI-assisted. Tool:
- [ ] A human read the generated diff

Context given to the tool:

## Security

- [ ] No keys, `.env`, or private data in the diff or the prompt
- [ ] Public input still goes through `NumberStrings.requireDigits`, or this PR does not touch input
