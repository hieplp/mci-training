plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'mci-web'

// Composite build: mci-web depends on mci-core without merging the two builds.
includeBuild '../mci-core'
